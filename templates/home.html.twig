{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div style="margin-bottom:40px;">
            <h1>Bienvenue sur la page d'accueil</h1>
        </div>
        <div>
            <a href="{{ path('list_orders') }}" class="btn btn-primary">Voir les commandes traitées</a>
            <a href="{{ path('get_new_orders') }}" class="btn btn-success">Récupérer les nouvelles commandes</a>
            <a href="{{ path('app_analytics') }}" class="btn btn-warning">Analytiques</a>
        </div>
    </div>
{% endblock %}
